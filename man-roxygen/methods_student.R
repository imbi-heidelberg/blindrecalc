#' @param design Object of class \code{Student} created by \code{setupStudent}.
#' @param n1 Either the sample size of the first stage (if
#'   \code{recalculation = TRUE} or the toal sample size (if
#'   \code{recalculation = FALSE}).
#' @param nuisance Value of the nuisance parameter. For the
#'   Student's t-test this is the variance.
